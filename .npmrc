# Expo's dependency graph regularly produces peer-dependency conflicts that npm 7+
# treats as fatal. Without this flag installs fail locally and, more painfully,
# inside EAS cloud builds where the error message points somewhere unrelated.
legacy-peer-deps=true
